<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page %></title>
<style>
    body{
        text-align: center;
        background-color: #92a7f3;
    }
    h1{
        font-size: 3em;
        text-decoration: underline;
        text-shadow: 2px 3px 2px ;
    }
    table {
        width: 100%;
        border: 2px groove #4e6c29;
        margin: auto;
        background-color: rgb(141, 182, 249);
        font-size: 1.5em;
    }
    th , tr ,td{
        border: 4px solid rgb(46, 46, 46);
    }
    p a{
        font-size: 1.5em;
        text-decoration: none;
        font-weight: 800;
    }
    h3{
        font-size: 2em;
        color:#1B03A3 ;
    }   
</style>
</head>
<body>
    <h1>Student Page</h1>
    <h3><%= title %></h3>
    <p><a href="/register">Register Here</a></p>

    <% if (students!= "") { %>

    <table>
        <tr>
            <th>Roll No</th>
            <th>Name </th>
            <th>Mail</th>
            <th>Age </th>
            <th>Operation</th>
        </tr>
      <% students.forEach((students,index) => { %>
        <tr>
            <td><%= index+1 %></td>
            <td><%= students.name %></td>
            <td><%= students.mail %></td>
            <td><%= students.age %></td>
            <td> <a href="/update/<%= students._id %>">Update</a> | <a href="/delete/<%= students._id %> ">Delete</a> </td>
        </tr>
   <% }) %>
    </table>
    <% } %>
</body>
</html>